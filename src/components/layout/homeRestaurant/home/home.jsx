import home from './home.module.css';

const Home = () => {
  return (
    <section className={`${home.home} lg:bg-cover w-full h-screen rounded-l-xl`}>
        
    </section>
  )
}

export default Home