import React from 'react'

const Button = ({ name }) => {
    return (
        <div>
            <button
                className=''

            >{name} click</button>
        </div>
    )
}

export default Button